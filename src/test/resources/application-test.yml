spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: 
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    show-sql: true
  application:
    name: Counterparty-api-test

server:
  port: 0

schema:
  counterparty-schema-path: schemas/counterparty-request-schema.json
